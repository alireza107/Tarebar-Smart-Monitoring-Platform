# Backward-compatible default for `docker compose up`.
# Equivalent to docker-compose.dev.yml. Prefer:
#   docker compose -f docker-compose.dev.yml --env-file .env.dev up --build
name: tarebar-dev

include:
  - path: docker-compose.dev.yml
    env_file:
      - .env.dev
